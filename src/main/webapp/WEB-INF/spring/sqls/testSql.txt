DROP SEQUENCE SPSEQ;
DROP TABLE SAMPLE;

CREATE SEQUENCE SPSEQ NOCACHE;
	

CREATE TABLE SAMPLE(
    SPNO NUMBER PRIMARY KEY,
	SPNAME VARCHAR2(60) NOT NULL,
	SPTITLE VARCHAR2(100) NOT NULL,
	SPCONTENT VARCHAR2(200) NOT NULL,
	SPDATE DATE NOT NULL
);

INSERT INTO MYBOARD VALUES(SPSEQ.NEXTVAL, '김수호','스프링 테스트','spring mvc',SYSDATE);

SELECT * FROM MYBOARD;

COMMIT;